// DATASET: blackangels from ./dataset/blackangels
const blackangelsPrompts = {
  getHighestRating() {
    // Find the album with the highest rating

    const result = 'REPLACE WITH YOUR RESULT HERE';
    return result;

    // Annotation:
    // Write your annotation here as a comment
  },

  getReleaseDate() {
    // Return an array of album titles with release dates after 2008, e.g.
    //['Phosphene Dream', 'Indigo Meadow', 'Death Song']

    const result = 'REPLACE WITH YOUR RESULT HERE';
    return result;

    // Annotation:
    // Write your annotation here as a comment
  },

  returnMatchingWord() {
    // Return an array of bestSongs that include the word 'song', e.g.
    //['entrance song', 'life song']

    const result = 'REPLACE WITH YOUR RESULT HERE';
    return result;

    // Annotation:
    // Write your annotation here as a comment
  },

  getColorfulAlbum() {
    // Return an array of albums with colorful album art, e.g. 
    //['Directions to See a Ghost', 'Phosphene Dream', 'Death Song']

    const result = 'REPLACE WITH YOUR RESULT HERE';
    return result;

    // Annotation:
    // Write your annotation here as a comment
  },

  getRatingSum() {
    // Sum up the rating value for all of the albums

    const result = 'REPLACE WITH YOUR RESULT HERE';
    return result;

    // Annotation:
    // Write your annotation here as a comment
  }
}